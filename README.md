# ESPy

Entity Set Programming for Python.

ESPy is an experimental framework for expressing business logic over sets of entities instead of explicit object iteration.

## Goals

- Entity-centric programming
- Set-based operations
- Backend-agnostic execution
- Intermediate Representation (IR)
- Query planning and optimization