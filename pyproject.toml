[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "espy"
version = "0.1.0"
description = "Entity Set Programming for Python"
requires-python = ">=3.12"

dependencies = [
    "sqlmodel",
]

[tool.setuptools.packages.find]
where = ["src"]